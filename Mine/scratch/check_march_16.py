import pandas as pd
import numpy as np
from src.trading_app.Backtest.apex_reversal_engine import ApexReversalBacktester
import json

# Assuming we have some data saved or we can mock a row
# But better to check the actual data if possible.
# Since I can't easily fetch Fyers data here, I'll check the engine logic again.


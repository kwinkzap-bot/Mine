"""
algo package.

NiftyWeeklyStraddle is exposed via nifty_weekly_straddle.py (a shim that loads
the implementation from Nifty_straddle_0.16_delta/ by file path, because that
directory name contains a dot and cannot be imported normally).
"""
